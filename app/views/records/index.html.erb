<div class="container-discover-card">
  <div class="slider-wrap">
    <div id="card-slider" class="slider">
      <% @discover.sample(10).each do |record| %>
      <div data-new-deal-url="<%= new_record_deal_path(record) %>" class="slider-item">
        <div class="animation-card_image">
            <img src="<%= record.release.image %>" alt="">
        </div>
      </div>
      <% end %>
    </div>
  </div>
  <div class="select-discover-card">
    <i class="fas fa-fast-backward" id="previous"></i>
    <%= link_to records_path do %>
      <i class="fas fa-eject"></i>
    <% end %>
    <a id="create-deal-btn" href="">
      <i class="fas fa-play" style="font-size: 350%;" id="new-deal"></i>
    </a>
    <i class="fas fa-fast-forward" id="next"></i>
  </div>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/2.1.3/TweenMax.min.js"></script>
<script src="js/TweenMax.min.js"></script>
<script src="js/TimelineMax.min.js"></script>

<div class="deal-requester-img">
  <span>Vous avez proposé cet échange à <%= record.user.username %></span>
  <img src="<%= user_avatar_path(record.user) %>">
</div>

<%= render 'mydeals/components/show/record', record: record %>

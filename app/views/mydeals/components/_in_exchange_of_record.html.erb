<p class="swapping-for-label">En échange de votre vinyl</p>
<div class="myrecords-card">
  <div id="my-records-infos">
    <img src="<%= record_cover(record) %>">
    <div>
      <h5><%= record.release.title %></h5>
      <p><%= record.release.artist %></p>
    </div>
  </div>
</div>
